name: vaksin_kasehat
description: A Vaccine management app.

#publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  google_fonts: ^1.1.1

  form_field_validator: ^1.0.1
  pin_input_text_field: ^3.1.1

  firebase_core: ^0.5.1
  firebase_auth: ^0.18.2
  firebase_analytics: ^6.1.0

  provider: ^4.3.2+2

  progress_dialog: ^1.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/images/
    - assets/logo/
